import { Container, Heading, Page, Text } from "@/components"

export default function AboutPage() {
  return (
    <Container>
      <Heading>About</Heading>
      <Text>
        Yes, zod is a TypeScript-first schema declaration and validation library
        that can handle error handling as part of its functionality. When you
        use zod.parse on an object, it will validate the object against the
        specified schema, and if there are any errors, it will throw an
        exception.
      </Text>
      <div>
        Yes, zod is a TypeScript-first schema declaration and validation library
        that can handle error handling as part of its functionality. When you
        use zod.parse on an object, it will validate the object against the
        specified schema, and if there are any errors, it will throw an
        exception.
      </div>
    </Container>
  )
}
